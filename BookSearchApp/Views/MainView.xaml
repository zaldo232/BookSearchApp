<Window x:Class="BookSearchApp.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:BookSearchApp.ViewModels"
        Title="책 검색 앱" Height="600" Width="800"
        xmlns:local="clr-namespace:BookSearchApp.Views">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>

    <DockPanel Margin="10">
        <!-- 검색창 -->
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0 0 0 10">
            <TextBox Width="300"
                     Margin="0 0 10 0"
                     Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}" />
            <Button Content="검색" Command="{Binding SearchCommand}" />
            <Button Content="즐겨찾기 보기" Margin="10 0 0 0" Click="OpenFavorites_Click" />
        </StackPanel>

        <!-- 책 리스트 -->
        <ListBox ItemsSource="{Binding Books}"
                 MouseDoubleClick="BookListBox_MouseDoubleClick">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border BorderBrush="Gray" BorderThickness="1" Padding="10" Margin="5">
                        <StackPanel Orientation="Horizontal">
                            <Image Source="{Binding Thumbnail}" Width="80" Height="100" Margin="0 0 10 0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Publisher}" />
                                <TextBlock Text="{Binding Authors[0]}" />
                                <TextBlock Text="{Binding Price, StringFormat='₩{0:N0}'}" />

                                <!-- 즐겨찾기 버튼 -->
                                <Button Content="★ 즐겨찾기"
                                        Margin="0 10 0 0"
                                        Command="{Binding DataContext.AddToFavoritesCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                            </StackPanel>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </DockPanel>
</Window>
